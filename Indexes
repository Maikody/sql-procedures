CREATE INDEX BOOK_TITLE ON BOOKS (TITLE);

EXPLAIN SELECT * FROM READERS
        WHERE FIRSTNAME LIKE 'John';

CREATE INDEX READER_NAME ON READERS (FIRSTNAME, LASTNAME);

EXPLAIN SELECT * FROM READERS
        WHERE FIRSTNAME LIKE 'John';
